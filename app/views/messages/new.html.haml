!!!
%html
  %body
    .contents
      = render template: "groups/index"
      .rightcontents
        .header
          .header__info
            %p.header__info__title #{ @group.name }
            .header__info__member
              %p Member：
              %ul
                - @users.each do |user|
                  %li #{ user.name }
          .header__edit
            = link_to "edit", edit_group_path(@group), class: "header__edit__button"
        .main
          .main__messages
            .main__message
            = render @messages
        .form
          .form__box
            = form_for [@group, @message], format: 'json', id: 'new_message' do |f|
              .form__box__left
                .form__box__text
                  = f.text_field :body, placeholder: "type a message", id: "input-body", class: "form__box__text"
                .form__box__pic
                  = f.label :image, fa_icon('picture-o')
                .form__box__pic__botton
                  = f.file_field :image
              .form__box__right
                .form__box__send
                  = f.submit "send", class: "form__send"

